import React from 'react';
import { Button } from 'react-native';

const Weather = () => {
    return(
        <Button
            title="Login"
            
        />
    );
}

export default Weather;